{
  "name": "promo-track-backend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "PromoTrack serverless backend: Midway-authorised review sharing and portfolio persistence (single SAM stack).",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node --import ./local/register-fake-dynamo.mjs local-server.mjs",
    "test": "cd .. && npx vitest run backend",
    "validate": "sam validate --lint",
    "build": "PATH=\"$PWD/node_modules/.bin:$PATH\" sam build",
    "deploy:beta": "npm run build && sam deploy --config-env beta",
    "deploy:prod": "npm run build && sam deploy --config-env prod"
  },
  "dependencies": {
    "aws-jwt-verify": "^4.0.1"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1097.0",
    "@aws-sdk/lib-dynamodb": "^3.1097.0",
    "cors": "^2.8.6",
    "esbuild": "^0.25.9",
    "express": "^5.2.1"
  }
}
